#include "moves.h"

void Moves::move_north(u64 board);
        void move_south(u64 board);
        void move_east(u64 board);
        void move_west(u64 board);
        void move_northeast(u64 board);
        void move_soutwest(u64 board);
        void move_southeast(u64 board);
        void move_northwest(u64 board);
        void move_knight(u64 board);
